// You still need to install clsx and tailwind-merge for this to work
// npm install clsx tailwind-merge

// import clsx from "clsx";
// import { twMerge } from "tailwind-merge";

// function cn(...inputs) {
//   return twMerge(clsx(...inputs));
// }

// export default cn;